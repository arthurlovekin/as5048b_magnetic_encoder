# AS5048B Magnetic Angle Sensor
[Original Datasheet](https://look.ams-osram.com/m/287d7ad97d1ca22e/original/AS5048-DS000298.pdf)

## General Description
The AS5048 is an easy to use 360° angle position sensor with a 14-bit high resolution output. The IC measures the absolute position of the magnet’s rotation angle and consists of Hall sensors, analog digital converter and digital signal processing. The absolute position information of the magnet is directly accessible over a PWM output and can be read out over a standard SPI or a high speed I²C interface.
AS5048A has a SPI interface, AS5048B I²C interface. Both devices offer a PWM output. The zero position can be programmed via SPI or I²C command. This simplifies the assembly of the complete system because the zero position of the magnet does not need to be mechanically aligned. The sensor tolerates misalignment, air gap variations, temperature variations and as well external magnetic fields. This robustness and wide temperature range (-40°C up to 150°C) of the AS5048 makes the IC ideal for rotation angle sensing in harsh industrial and medical environments. Several AS5048 ICs can be connected in daisy chain for serial
data read out. An internal voltage regulator allows the AS5048 to operate at either 3.3V or 5V supplies.

## I2C Interface
The AS5048B supports 2-wire high-speed I²C protocol in device
mode. The host MCU (master) has to initiate the data transfers. The 7-bit device address of the slave depends on the state of the OTP I²C register 21 (0x15) bit 0… 4 + 2 I²C address selection pin 3 and 4.
Supported modes:
• Random/Sequential Read
• Byte/Page Write
• Standard : 0 to 100kHz clock frequency (slave mode)
• Fast Mode : 0 to 400kHz clock frequency (slave mode)
• High Speed: 0 to 3.4MHz clock frequency (slave mode)
The SDA signal is bidirectional and is used to read and write the serial data. The SCL signal is the clock generated by the host MCU, to synchronize the SDA data in read and write mode. The maximum I²C clock frequency is 3.4MHz, data are triggered on the rising edge of SCL.

## I2C Register Table
| Address hex | Name | Description |
|-------------|------|-------------|
| 0x03        | Programming Control | Programming control register. Programming must be enabled before burning the fuse(s). After programming is an verification mandatory. See programming procedure. |
| 0x15 | I2C Slave Address | I2C slave address slave address consist of 5 bits (MSBs) and the hardware setting of Pins A1 and A2 I²C address <4> is the inversion defined as '1' |
|0x16 | OTP Register Zero Position Hi | Zero Position value high byte |
|0x17 | OTP Register Zero Position Lo 6 LSBs | Zero Position remaining 6 lower LSB's |
| 0xFA | Automatic Gain Control | Automatic Gain Control
value. 0 decimal represents high magnetic field 255 decimal represents low magnetic field |
| 0xFB | Diagnostics | Diagnostic flags |
| 0xFC 0xFD | Magnitude | Magnitude value of the CORDIC |
| 0xFE 0xFF | Angle | Angle output valude including zero position correction |

(Some rows and columns are omitted above; the relevant ones are expanded below)


## I2C Slave Address
(Details Summarized from Fig. 31 Resgister Map I2C, and Fig. 32 Slave Address Construction)
The 7-bit I2C slave address is derived from a combination of the OTP Register 0x15 and the hardware setting of Pins A1 and A2:

| Bit | Description | Default |
|-----|--------|---------|
| 6 | OTP 0x15 bit 4 (inverted) | 1 |
| 5 | OTP 0x15 bit 3 | 0 |
| 4 | OTP 0x15 bit 2 | 0 |
| 3 | OTP 0x15 bit 1 | 0 |
| 2 | OTP 0x15 bit 0 | 0 |
| 1 | Pin A1 | 0 |
| 0 | Pin A2 | 0 |

The slave address consists of the hardware setting on pins A1, A2. The MSB of the slave address (yellow) is internally inverted. This means that by default the resulting data is ‘1’. A read of the I²C slave address register 21 will return a ‘0’ at the MSB.

## Slave Command Construction
(Details summarized from Figure 32: Slave Command Construction)
A random read consists of:
| Start (1 bit) | OTP content (5 bits) | HW pins (2 bits) | Read/Write (1 bit) |
| ACK (1 bit) | Register Address (8 bits) | ACK (1 bit) | 
| Start (1 bit) | OTP content (5 bits) | HW pins (2 bits) | Read/Write (1 bit) |
| ACK (1 bit) | Dat Byte (n) (8 bits) | ACK (1 bit) | Stop (1 bit) |

A Random Write consists of:
| Start (1 bit) | OTP content (5 bits) | HW pins (2 bits) | Read/Write (1 bit) |
| ACK (1 bit) | Register Address (8 bits) | ACK (1 bit) | 
| Data Byte (n) (8 bits) | ACK (1 bit) | Stop (1 bit) |

## Programming Control
(Details summarized from Figure 31: Register Map I2C)
The Programming Control register 0x03 is used to program the device. It is a 8-bit register with the following bits (each defaults to 0):

| Bit | Description |
|-----|-------------|
| 7 | Not Used |
| 6 | Verify |
| 5 | Not Used |
| 4 | Not Used |
| 3 | Burn | 
| 2 | Reserved |
| 1 | Reserved |
| 0 | Programming Enable |

## Angle Read Register
(Details summarized from Figure 31: Register Map I2C)
The read-only registers 0xFE and 0xFF are used to read the angle value. The angle value itself is 14 bits. All angle bits default to 0.

| Bit | Description |
|-----|-------------|
| 0xFE 7 |Angle <13> |
| 0xFE 6 | Angle <12> |
| ... | ... |
| 0xFE 0 | Angle <6> |
| -------| -----------|
| 0xFF 7 | Not Used |
| 0xFF 6 | Not Used |
| 0xFF 5 | Angle<5> |
| 0xFF 4 | Angle<4> |
| ... | ... |
| 0xFF 0 | Angle<0> |

## Diagnostic Register
(Details summarized from Figure 31: Register Map I2C)
The Diagnostic Register 0xFB is a read-only register with the following bits:
| Bit | Description | Default |
|-----|-------------| ------- |
| 7 | Not Used | n/a |
| 6 | Not Used | n/a |
| 5 | Not Used | n/a |
| 4 | Not Used | n/a |
| 3 | Comp High | 0 |
| 2 | Comp Low | 0 |
| 1 | COF | 0 |
| 0 | OCF | 1 |

## Diagnostic Functions of the AS5048
The AS5048 provides diagnostics functions of the IC and also diagnostic functions of the magnetic input field
Following diagnostic flags are available:
See Figure 31 register address 251 dec (AS5048B)
• OCF (Offset Compensation Finished), logic high indicates the finished Offset Compensation Algorithm. After power up the flag remains always to logic high.
• COF (CORDIC Overflow), logic high indicates an out of range error in the CORDIC part. When this bit is set, the angle and magnitude data is invalid. The absolute output maintains the last valid angular value.
• COMP low, indicates a high magnetic field. It is recommended to monitor in addition the magnitude value.
• COMP high, indicated a weak magnetic field. It is recommended to monitor the magnitude value.

##Programming of the AS5048
### Programming of the Zero Position: 
The absolute angle position can be permanent programmed over the interface. This could be useful for random placement of the magnet on the rotation axis. A readout at the mechanical zero position can be performed and written back into the IC. With permanent programming the position is non-reversible stored in the IC. This programming can be performed only once. To simplify the calculation of the zero position it is only needed to write the value in the IC which was read out before from the
angle register.

###Programming Sequence with Verification: 
To program the zero position is needed to perform following sequence:
1. Write 0 into OTP zero position register to clear
2. Read angle information
3. Write previous read angle position into OTP zero position register
Now the zero position is set.
If you want to burn it to the OTP register send:

4. Set the Programming Enable bit in the OTP control register
5. Set the Burn bit to start the automatic programming procedure
6. Read angle information (equals to 0)
7. Set the Verify bit to load the OTP data again into the internal registers
8. Read angle information (equals to 0)

The programming can either be performed in 5V operation using the internal LDO, or in 3V operation but using a minimum supply voltage of 3.3V. In case of 3V operation, also a 10μF capacitor is required on the VDD3 pin.

## Programming the I²C Slave address: 
(Summary of [Application Note: AS5048B I2C Slave Address Programming](https://look.ams-osram.com/m/5a6027bb95c07463/original/AS5048-AN000386.pdf))

Assume we start with a single sensor on the default address (0x40). We want to give it a new address (0x44). Recall that the Address is 7 bits, of which the 5 MSB are programmable in software, and the 2 LSB are from hardware. Therefore if we program 0x01 into the 5 MSB, we get 0b0100,0100, which is 0x44.

We have a function I2CWrite8(slave_address: u8, register_address: u8, data: u8) that writes 8 bits to the slave address at the register address with the data.

1. Set new slave address: Write new slave address into I2C slave address register (hex.15, dec.21)
    - I2CWrite8(0x40, 0x15, 0x01)
2. Enable special programming mode: Write dec.253 (hex.FD) into the OTP control register (dec.3) to enable the special programming
mode
    - I2CWrite8(0x44, 0x03, 0xFD)
3. Enable automatic programming procedure: Set the Burn bit (dec.8) in the OTP control register (dec.3) to enable automatic programming procedure
    - I2CWrite8(0x44, 0x03, 0x08)
4. Disable special programming mode: Write dec.0 into the OTP control register (dec.3) to disable the special programming mode
    - I2CWrite8(0x44, 0x03, 0x00)

Now the new slave address is programmed.